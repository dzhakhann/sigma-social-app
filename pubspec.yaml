name: sigma_social_app
description: "Sigma Social Network - Flutter Mobile App"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  socket_io_client: ^2.0.0
  cupertino_icons: ^1.0.6
  image_picker: ^1.0.4
  http_parser: ^4.0.2
  video_player: ^2.8.3
  cached_network_image: ^3.3.1
  timeago: ^3.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
